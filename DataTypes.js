let length = 16;           // Tipe data: Number (angka)
let lastname = "Johnson";  // Tipe data: String (teks)
const x = {                // Tipe data: Object (objek)
      firstname: "John",   // Properti dalam objek, bertipe String
      lastname: "Doe"      // Properti dalam objek, bertipe String
};
